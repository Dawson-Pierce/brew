#include "brew/desktop/plot_utils/plot_trajectory_gaussian.hpp"

// Implementations are template inline in the header; this TU exists only to keep
// the build graph unchanged after the Trajectory<> MaxWindow refactor.

namespace brew::plot_utils {
} // namespace brew::plot_utils
